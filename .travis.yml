language: ruby

notifications:
  email:
    recipients:
      - delabriere@imsb.biol.ethz.ch
    on_success: always
    on_failure: always

services:
  - docker

before_install:
script:
  - docker build -t lcms_workflow_zamboni:latest .
  - docker run lcms_workflow_zamboni:latest -v tests/input:/input -v test/output:/output -e POLARITY=positive -e USERNAME=test lcms_worfklow_zamboni
